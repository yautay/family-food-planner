Security+validation+ORM migration status:\n- Stage 1: API hardening (Helmet, CORS allowlist, body limits, auth rate limits).\n- Stage 2: Zod validation middleware across API routes.\n- Stage 3: Full backend runtime migration in src/controllers and src/services to Sequelize ORM (auth, RBAC, audit, tags, units, products, packages, ingredients, day plans, shopping lists, recipes, meal plans).\n- Removed legacy src/db/catalog.js better-sqlite3 client from runtime code.\n- Verification: npm run test:unit && npm run test:integration passing after each major stage.\nRemaining (optional): scripts/*.mjs still use better-sqlite3/raw SQL and can be migrated in a follow-up if desired.
